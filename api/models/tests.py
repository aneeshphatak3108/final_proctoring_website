class Test:
    def __init__(self, name, parent_url, duration_minutes, login_window_start, login_window_end, instructions, created_at, admin_info, students):
        self.name = name
        self.parent_url = parent_url
        self.duration_minutes = duration_minutes
        self.login_window_start = login_window_start
        self.login_window_end = login_window_end
        self.instructions = instructions
        self.created_at = created_at
        self.admin_info = admin_info
        self.students = students